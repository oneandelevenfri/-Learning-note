1* functions
注意缩进 function name + brackets parameters
def say_hi(name):
    print("Hello User "+name)

say_hi("mike")
return

2*if statement
is_male = True
is_tall = False
if is_male and is_tall:
    print("you are a male and tall")
elif is_male and not is_tall:
    print("you are a male but not tall")
elif not is_male and is_tall:
    print("you are not a male but tall")
else:
    print("you are neither a male or tall")
    
3*comparasion
字符间隙有空格
tab和blank space不能混用
函数下空两行
最后一行回车
def max_num(num1, num2, num3): 
    if num1 >= num2 and num1 >= num3:
        return num1
    elif num2 >= num1 and num2 >= num3:
        return num2
    else:
        return num3
*字符串是否相等
def e_pro(num1, num2):
    if num1 == num2:
        return "yes"
    else:
        return "no"


a1 = input()
a2 = input()

print(e_pro(a1, a2))

    
    
print(max_num(3, 4, 5))

4*输入数字

